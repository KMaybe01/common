# 面向对象设计停车场管理系统

> 使用面向对象思想设计停车场管理系统，包含：停车位、停车位提示牌、车辆等核心类，支持车辆进出停车场并实时更新剩余车位。

## Mermaid 流程图

```mermaid
flowchart TD
    subgraph 类关系
        A[ParkingLot 停车场] --> B[ParkingSpace 停车位]
        A --> C[DisplayBoard 展示牌]
        D[Car 车辆] --> A
    end

    subgraph 停车流程
        E[car.inPark park] --> F{ParkingLot 有空位?}
        F -->|否| G[打印"停车位已满"]
        F -->|是| H{车辆已在停车场?}
        H -->|是| I[打印"车辆已在"]
        H -->|否| J[遍历 parkSites 找空位]
        J --> K[找到空位: site.car = null]
        K --> L[car.site = 该车位]
        L --> M[leftSites--]
        M --> N[showLeftSites 更新展示牌]
    end

    subgraph 出场流程
        O[car.outPark park] --> P{car.site 为 null?}
        P -->|是| Q[打印"本来就没在停车场"]
        P -->|否| R[遍历 parkSites 找对应车辆]
        R --> S[site.car = null]
        S --> T[car.site = null]
        T --> U[leftSites++]
        U --> V[showLeftSites 更新展示牌]
    end
```

## 源代码

```javascript
/*
 *题目要求
 *使用面相对象设计一个停车场管理系统，该停车场包含：
 *    1.停车位，用于停放车辆；
 *    2.停车位提示牌，用于展示剩余停车位；
 *可以丰富该系统的元素，给出类，类属性，类接口。
 */

// 停车场类
class ParkingLot {
    constructor(n) {
        // 停车位
        this.parkSites = []
        // 剩余停车位个数
        this.leftSites = n
        this.board = new DisplayBoard()
        // 初始化停车位
        for (let i = 1; i <= n; i++) {
            this.parkSites.push(new ParkingSpace(i))
        }
        console.log('停车场初始化完毕')
        this.showLeftSites()
    }

    // 进停车场停车
    inPark(car) {
        if (this.leftSites === 0) {
            console.log('停车位已满')
            return
        }
        if (car.site) {
            console.log(car.carId + '车辆已经在停车场了')
            return
        }
        let len = this.parkSites.length
        for (let i = 0; i < len; i++) {
            const site = this.parkSites[i]
            // 如果停车位是空的
            if (site.car === null) {
                site.car = car
                car.site = site
                this.leftSites--
                console.log(car.carId + '车辆停入' + site.id + '号停车位')
                this.showLeftSites()
                return
            }
        }
    }

    // 出停车场
    outPark(car) {
        if (car.site === null) {
            console.log(car.carId + '本来就没在停车场')
            return;
        }
        let len = this.parkSites.length
        for (let i = 0; i < len; i++) {
            const site = this.parkSites[i]
            // 如果停车位是空的
            if (site.car.carId === car.carId) {
                site.car = null
                car.site = null
                this.leftSites++
                console.log(car.carId + '车辆已从' + site.id + '号停车位，出停车场')
                this.showLeftSites()
                return
            }
        }
    }

    // 显示剩余的停车位数量
    showLeftSites() {
        this.board.showLeftSapce(this.leftSites)
    }

}


// 停车位
class ParkingSpace {
    constructor(id) {
        // 停车位编号
        this.id = id
        // 停入的车辆
        this.car = null
    }
}

// 车 类
class Car {
    constructor(carId) {
        // 车牌号
        this.carId = carId
        // 停入的车位
        this.site = null
    }
    // 进入停车场
    inPark(park) {
        park.inPark(this)
    }
    outPark(park) {
        park.outPark(this)
    }
}

// 展示牌类
class DisplayBoard {
    constructor() {}
    // 展示剩余停车位
    showLeftSapce(n) {
        console.log(`当前剩余${n}个停车位`)
    }
}


const park = new ParkingLot(3)

const car1 = new Car('京A1XXX')
const car2 = new Car('京A2XXX')
const car3 = new Car('京A3XXX')
const car4 = new Car('京A4XXX')
car1.inPark(park)
car1.inPark(park)
car1.outPark(park)
car1.outPark(park)
car1.inPark(park)

car2.inPark(park)
car3.inPark(park)
car4.inPark(park)

car2.outPark(park)
car4.inPark(park)

console.log(park.parkSites)
```

## 逐行解析

### ParkingLot 停车场类
- **`constructor(n)`**：初始化 `n` 个停车位，创建 `DisplayBoard` 实例，打印初始化信息并展示剩余车位。
- **`inPark(car)`**：车辆入场逻辑。先判断是否有空位（`leftSites === 0`），再判断车辆是否已在停车场（`car.site` 不为 null）。然后遍历 `parkSites` 找到第一个空位，将车停入，`leftSites--`，更新展示牌。
- **`outPark(car)`**：车辆出场逻辑。判断车辆是否在场内，遍历找到对应车辆所在车位，清空车位和车辆的关联，`leftSites++`，更新展示牌。

### ParkingSpace 停车位类
- **`id`**：停车位编号。
- **`car`**：停入的车辆对象，初始为 null。

### Car 车辆类
- **`carId`**：车牌号。
- **`site`**：当前停放的车位对象，初始为 null。
- **`inPark(park)` / `outPark(park)`**：委托给 `ParkingLot` 的对应方法。

### DisplayBoard 展示牌类
- **`showLeftSapce(n)`**：打印当前剩余车位数。

### 测试用例
1. 创建 3 个车位的停车场。
2. `car1` 进入、再次进入（提示已在）、出场、再次出场（提示没在）、重新进入。
3. `car2`、`car3` 进入，`car4` 进入（车位已满，提示无空位）。
4. `car2` 出场，`car4` 进入（空出一个车位）。
5. 最终打印所有车位状态。

## 复杂度分析

| 维度 | 复杂度 | 说明 |
|------|--------|------|
| 时间复杂度 | O(n) — 入场/出场 | 需要遍历 `parkSites` 查找空位或匹配车辆 |
| 空间复杂度 | O(n) | `parkSites` 存储 n 个车位 |
| 优化空间 | O(1) | 可用 `Map<carId, site>` 实现 O(1) 查找 |
